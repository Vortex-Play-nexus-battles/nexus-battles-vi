package com.nexusbattles.ms_identidad.onboarding.model;

/** Estado de un paso del alta: pendiente, hecho (el servicio dueno dijo que si) o con error. */
public enum EstadoPaso {
    PENDIENTE,
    HECHO,
    ERROR
}
